import { TableBody, TableBodyProps } from '@mui/material';
import React from 'react';

const AtTableBody: React.FunctionComponent<TableBodyProps> = (props) => {
  return <TableBody {...props} />;
};

export default AtTableBody;
